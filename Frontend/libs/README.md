# Shared frontend libraries

This folder holds shared code for both Angular clients:

- `api-client`
- `contracts`
- `auth-core`
- `finance-core`
- `app-errors`
- `ui-kit`

Keep these libraries app-agnostic and expose public APIs through each `src/index.ts`.
